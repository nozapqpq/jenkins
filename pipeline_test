pipeline {
    agent any
    stages {
        stage('build') {
            sh 'gcc -o output run.c'
        }
        stage('run') {
            sh './output'
        }
        stage('check') {
            sh 'python3 check.py output.txt'
        }
    }
}
